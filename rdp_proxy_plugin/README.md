# 🔍 Плагин прокторинга для обнаружения RDP подключений

Этот проект представляет собой веб-плагин для системы тестирования, который может обнаруживать удаленные подключения (RDP) без необходимости установки дополнительного программного обеспечения на стороне пользователя.

## 🎯 Особенности

- **Веб-интерфейс**: Работает прямо в браузере без установки ПО
- **Обнаружение RDP**: Использует косвенные методы для выявления удаленных подключений
- **Анализ поведения**: Отслеживает движения мыши, нажатия клавиш и паттерны взаимодействия
- **Анализ видео**: Использует веб-камеру для обнаружения артефактов сжатия RDP
- **Реальное время**: Мониторинг происходит в режиме реального времени
- **Серверная часть**: Flask API для обработки предупреждений и статистики

## 🏗️ Архитектура

### Клиентская часть (JavaScript)
- `RDPDetector` - основной класс для обнаружения RDP
- Анализ поведения пользователя
- Мониторинг качества видео
- Обнаружение виртуальных машин
- Анализ сетевых характеристик

### Серверная часть (Python Flask)
- Обработка предупреждений от клиентов
- Хранение данных в SQLite
- API для статистики и мониторинга
- Дашборд для администраторов

## 🔍 Методы обнаружения RDP

### 1. Анализ поведения пользователя
- **Движения мыши**: Обнаружение роботизированных паттернов
- **Нажатия клавиш**: Анализ скорости и равномерности набора
- **Временные интервалы**: Выявление характерных задержек RDP

### 2. Анализ качества видео
- **Артефакты сжатия**: RDP использует специфические алгоритмы сжатия
- **Цветовая палитра**: Ограниченная палитра в удаленных сессиях
- **Разрешение**: Стандартные разрешения RDP

### 3. Обнаружение виртуальных машин
- **WebGL анализ**: Проверка рендереров и драйверов
- **Системные характеристики**: Анализ доступных ресурсов
- **API ограничения**: Проверка доступных функций браузера

### 4. Сетевой анализ
- **Задержки**: Измерение времени отклика
- **Переподключения**: Мониторинг изменений соединения
- **Качество соединения**: Анализ стабильности

## 🚀 Установка и запуск

### Требования
- Python 3.7+
- Современный браузер с поддержкой WebRTC
- Веб-камера и микрофон (для полной функциональности)

### Установка серверной части
```bash
# Клонирование репозитория
git clone <repository-url>
cd rdp_proxy_plugin

# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера
python server_example.py
```

### Использование клиентской части
1. Откройте `index.html` в браузере
2. Разрешите доступ к камере и микрофону
3. Нажмите "Запустить мониторинг"
4. Система начнет отслеживать подозрительную активность

## 📡 API Endpoints

### POST /api/rdp-alert
Отправка предупреждения о RDP от клиента
```json
{
  "message": "Обнаружена подозрительная активность",
  "confidence": 0.85,
  "evidence": [...],
  "timestamp": 1640995200000
}
```

### GET /api/session/{session_id}
Получение информации о сессии

### GET /api/dashboard
Дашборд с общей статистикой

### GET /api/alerts/recent
Последние предупреждения

## ⚠️ Ограничения

### Технические ограничения
- Браузеры ограничивают доступ к системным ресурсам
- Обнаружение основано на косвенных признаках
- Возможны ложные срабатывания
- Требуется согласие пользователя на доступ к камере/микрофону

### Правовые аспекты
- Необходимо информирование пользователей о мониторинге
- Соблюдение GDPR и других регуляций
- Получение согласия на обработку данных
- Прозрачность методов обнаружения

## ⚙️ Настройка

### Пороги обнаружения
```javascript
// В web_rdp_detector.js
this.detectionThreshold = 0.7; // Порог подозрительности (0-1)
```

### Серьезность предупреждений
```python
# В server_example.py
def calculate_severity(type):
    severity_map = {
        'RDP_COMPRESSION_ARTIFACTS': 0.8,
        'SUSPICIOUS_MOUSE_PATTERN': 0.6,
        # ...
    }
```

## 🛠️ Разработка

### Добавление новых методов обнаружения
1. Создайте новый метод в классе `RDPDetector`
2. Добавьте вызов в `initialize()`
3. Обновите `calculateSeverity()` для нового типа предупреждения
4. Протестируйте на различных сценариях

### Интеграция с существующими системами
- Используйте API endpoints для интеграции
- Настройте webhook'и для уведомлений
- Адаптируйте под вашу систему аутентификации

## 🔒 Безопасность

### Рекомендации
- Используйте HTTPS для всех соединений
- Регулярно обновляйте зависимости
- Ограничьте доступ к API
- Логируйте все подозрительные активности
- Регулярно очищайте старые данные

### Мониторинг
- Отслеживайте производительность системы
- Анализируйте ложные срабатывания
- Обновляйте алгоритмы обнаружения
- Собирайте обратную связь от пользователей

## 📄 Лицензия

MIT License - см. файл LICENSE для подробностей.

## 🆘 Поддержка

Для вопросов и предложений создавайте issues в репозитории проекта.
